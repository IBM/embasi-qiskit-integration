# Byte-compiled / optimized / cache
__pycache__/
*.py[cod]
*$py.class

# Distribution / packaging
build/
dist/
*.egg-info/
*.egg

# Virtual environments
.venv/
venv/
env/

# uv
.uv/

# Testing / coverage
.pytest_cache/
.coverage
.coverage.*
htmlcov/
coverage.xml
.mypy_cache/
.ruff_cache/

# Job artifacts produced by the CLI handoff
*.ERROR

# Editors / OS
.idea/
.vscode/
*.swp
.DS_Store
# Emacs backups / autosaves.  Upstream EmbASI has committed these before
# (`ks_array.py~`, `#qmcode_adapters.py#` ship inside the installed package), so
# guard our own tree against the same slip.
*~
\#*\#
.\#*

# Local scratch notes and generated docs, deliberately untracked.
/EMBEDDING_WORKFLOW_NOTES.md
/docs/
/port.md
/TODO.md
